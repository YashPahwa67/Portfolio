# Optional: a GitHub personal access token (classic, NO scopes needed for public data).
# Without it the app uses unauthenticated requests (60/hour per IP), which is fine for
# a portfolio. Add a token only if you hit rate limits during heavy local testing.
# Copy this file to ".env.local" and fill in the value. NEVER commit your real token.
VITE_GITHUB_TOKEN=
